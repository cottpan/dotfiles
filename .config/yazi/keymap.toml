# yazi のキー割り当て。既定はほぼ vim なので、揃っていないものだけ足す。
#
# この dotfiles の約束: 移動 = j/k, 閉じる = q, 検索 = /, ヘルプ = ?
# yazi の既定は移動 / 閉じる / 検索は揃っているが、ヘルプだけ ~ と F1 なので ? を足す。
# prepend_keymap は既定より先に見られるので、既定はそのまま残る。

[[mgr.prepend_keymap]]
on = "?"
run = "help"
desc = "ヘルプ (キー一覧)"
